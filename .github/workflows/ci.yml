name: CI

on:
  push:
    branches-ignore:
      - master
  pull_request:
    branches:
      - '**'

jobs:
  build-test-pack:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@v3.0.0
        with:
          versionSpec: '6.0.0'

      - name: Determine Version
        id: gitversion
        shell: bash
        run: |
          VERSION=$(dotnet gitversion /output json /showvariable SemVer)
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Restore dependencies
        run: dotnet restore --artifacts-path=/tmp/artifacts/qbittorrent-apiclient

      - name: Build
        run: dotnet build --configuration Release --no-restore --artifacts-path=/tmp/artifacts/qbittorrent-apiclient

      - name: Run tests
        run: dotnet test --configuration Release --no-build --artifacts-path=/tmp/artifacts/qbittorrent-apiclient

      - name: Pack
        run: dotnet pack src/QBittorrent.ApiClient/QBittorrent.ApiClient.csproj --configuration Release --no-build --artifacts-path=/tmp/artifacts/qbittorrent-apiclient

      - name: Upload package artifacts
        uses: actions/upload-artifact@v4
        with:
          name: qbittorrent-apiclient-${{ env.VERSION }}
          path: /tmp/artifacts/qbittorrent-apiclient/package/release/*
