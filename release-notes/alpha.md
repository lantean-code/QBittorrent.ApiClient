# QBittorrent.ApiClient {{VERSION}}

## Summary

Alpha build of `QBittorrent.ApiClient`.

This prerelease is intended for early validation of the standalone qBittorrent Web API client package.

## Installation

```bash
dotnet add package QBittorrent.ApiClient --version {{NUGET_VERSION}}
```
